<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.kafka_chat.chat.ChatMapper">
    <insert id="insertChatMessage">
        INSERT INTO chat_message (sender, message, timestamp)
        VALUES (#{sender}, #{message}, #{timestamp})
    </insert>

    <select id="getAllMessages" resultType="com.example.kafka_chat.chat.ChatMessage">
        SELECT * FROM chat_message ORDER BY id ASC
    </select>
</mapper>
